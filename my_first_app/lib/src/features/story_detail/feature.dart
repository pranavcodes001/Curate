// Story Detail feature public surface
// Responsibility: Expose story detail models, summary view contract, and presentation entry points.
// Note: This feature coordinates read-only summary endpoint usage (exploration is opt-in elsewhere).
