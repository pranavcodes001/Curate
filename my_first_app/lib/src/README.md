lib/src overview

- `core/` — App-level shared concerns (routing, models, guards)
- `features/` — Feature-first modules. Each feature is isolated and contains data/domain/presentation subfolders.

This workspace currently contains placeholder files only — no UI or implementation yet.